{
  "name": "@sarveshsea/memi-studio-types",
  "version": "0.1.0",
  "description": "Public type contracts for the Mémoire Studio runtime sidecar — ProviderRuntime events, branded entity IDs, typed errors, state machine alphabets. Consumed by github.com/sarveshsea/memi-studio's frontend so the GUI app speaks the same protocol the engine emits.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./contracts": "./src/contracts.ts",
    "./state": "./src/state.ts",
    "./rpc": "./src/rpc.ts"
  },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc --noEmit"
  },
  "files": ["dist", "src"],
  "keywords": ["memoire", "memi-studio", "agent-runtime", "types"],
  "license": "MIT",
  "author": "Sarvesh Chidambaram",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sarveshsea/m-moire.git",
    "directory": "packages/memi-studio-types"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
